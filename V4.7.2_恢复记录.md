# 大可桌边 V4.7.2 恢复记录

日期：2026-09-20

本轮版本：V4.7.2

## 从现有文件确认的状态

- 上一次 Git 提交是 `257cbf3`（V4.7.0，2026-09-17）。没有发现 Git stash 中的额外改动。
- 中断后留下的 11 个已修改文件，主要在做 V4.7.1：将“继续处理”结果提示放在操作按钮旁；让复杂重要提醒与简单提醒的状态更明确；加宽 Windows 的提醒时间选择框；同步更新版本号、测试与安装配置。
- 这些改动原本没有提交。恢复时保留了它们，没有回退到 V4.7.0。

## 本次补齐

- 复制后立即在右侧编辑器加载新事项，保证右侧内容与当前选中 ID 一致。
- 复制旧事项到今天时跳过已经过去的提醒，并立即刷新 Windows 额外通知计划。
- 成功提示改用可取消、可重启的定时器；切换或清空事项时清除旧提示，避免提示串到另一条事项。
- 操作暂时不可重复时，按钮提示说明原因；复制说明不再误称未来事项一定会复制到今天。
- 四个截图问题逐项核对：周期提醒会清除外部简单提醒选中态；按钮焦点样式已有去除规则；继续处理说明已收进按钮提示；普通事项与重要提醒共用 88px 时间框宽度，确保数字、中文单位和下拉箭头完整。
- V4.7.2 将三种重要提醒方式改为“查看与设置”和“选中生效”分开：只能有一种被选中；未选中的方式仍可点击和编辑，但整块界面会置灰；尝试选中另一个方式时显示“提醒方式已经被选中了”；再次点击“已选中”可先释放当前方式，再选择新的方式。三种方式的设置分别保存，即使未选中也不会丢失。
- 后续界面调整将三个分类按钮旁边的独立“选中”合并为右侧固定的集体按钮；内容改用固定高度的堆叠页，切换分类不再触发弹窗重新定位，周期自定义行只在弹窗内部展开。
- 增加了复制后载入新事项、步骤进度重置、过期提醒处理和切换事项清除提示的回归测试。

## 验证与待办

- Mac 上以 Qt 离屏模式运行 80 项测试，全部通过。
- 使用隔离的临时数据目录启动主窗口成功，窗口标题为“大可桌边 V4.7.2”。
- `git diff --check` 通过。尚未在 Windows 上验证实际界面、系统提醒和安装包。
